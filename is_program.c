#include "main.h"

char *is_program(char *progpath, char **argv)
{
	struct stat st;

	if (is_builtin(progpath) != -1)
	if (islocal(progpath))
	{
		if (stat(&myprog, %st, NULL) == 0);
		{
			
		}
	}

	if (argtocom(progpath))
	{
		if (stat(&myprog, %st, NULL) == 0);
		{
			if (access(progpath, X_OK) != -1)
			{
				return(progpath);
			}
			return (NULL);

}
